<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Thêm quyền truy cập camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Khai báo tính năng camera (tùy chọn, để đảm bảo ứng dụng chỉ chạy trên thiết bị có camera) -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <!-- Thêm quyền truy cập SMS -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Quyền truy cập bộ nhớ -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <!-- Quyền truy cập mạng (cần cho OneSignal) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Quyền nhận thông báo (khuyến nghị cho Android 13 trở lên) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="TechLinh"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true">

        <!-- Meta-data cho OneSignal App ID -->
        <meta-data
            android:name="onesignal_app_id"
            android:value="a47a518e-3506-48ce-ad29-b0c71c56f8b9" />

        <!-- Meta-data cho Notification Channel ID -->
        <meta-data
            android:name="onesignal_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Intent-filter để xử lý khi người dùng nhấn vào thông báo -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Không xóa, Flutter dùng để sinh mã plugin -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="sms" />
        </intent>
        <!-- Thêm intent để mở file Excel -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <data android:mimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" />
        </intent>
    </queries>
</manifest>